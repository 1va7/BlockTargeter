import { Command } from '@oclif/core';
export default class Run extends Command {
    static description: string;
    static enableJsonFlag: boolean;
    static flags: {
        scriptArgs: import("@oclif/core/lib/interfaces").OptionFlag<string[] | undefined, import("@oclif/core/lib/interfaces/parser").CustomOptions>;
    };
    static args: {
        script: import("@oclif/core/lib/interfaces/parser").Arg<string, Record<string, unknown>>;
    };
    run(): Promise<{
        output: string;
    }>;
}
